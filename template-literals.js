let names = "Japheth";
console.log(`Hello ${names}`);

// Intecting some code
function getReasonCount(){
    return 4; // or 2\ for the second argument
}

let concFunction = `Give me ${(getReasonCount() == 1 ? 'one good reason' : 'a lot of reasons')} to try this`;
console.log(concFunction);